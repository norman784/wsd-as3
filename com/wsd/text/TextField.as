﻿package com.wsd.text{	import com.wsd.Base	import com.wsd.View		import flash.text.TextField	import flash.text.TextFormat	import flash.text.TextFormatAlign		public class TextField extends View	{		private var textField:flash.text.TextField = new flash.text.TextField				public function TextField(options:Object = null)		{			var txtFormat:TextFormat = new TextFormat()						if (options == null)			options = new Object						switch(options.align) {				case 'right':					options.align = TextFormatAlign.RIGHT				break;				case 'left':					options.align = TextFormatAlign.LEFT				break;				default:					options.align = TextFormatAlign.CENTER				break;			}						txtFormat.size = (isNaN(parseInt(options.size)) ? 20 : options.size)			txtFormat.align = options.align						if (options.font != null)	txtFormat.font = options.font						textField.defaultTextFormat = txtFormat			textField.textColor 	= (isNaN(parseInt(options.color)) 		? options.color : 0x000000)			textField.selectable 	= (options.selectable != null 			? options.selectable : false)			textField.wordWrap 		= (options.wordWrap != null 			? options.wordWrap : false)			textField.border 		= (options.border != null 				? options.border : false)			textField.width 		= (isNaN(parseInt(options.width)) 		? options.width : 450)			textField.height 		= (isNaN(parseInt(options.height)) 		? options.height : 50)			x 						= (isNaN(parseInt(options.x)) 			? options.x : x)			y 						= (isNaN(parseInt(options.y)) 			? options.y : y)						addChild(textField)		}				public function set(text:String):void		{			textField.text = text						Base.log('TextField::set(' + textField.text + ')')		}				public function appendText(text:String):void		{			textField.appendText(text)						Base.log('TextField::set(' + textField.text + ')')		}				public function get():String		{			return textField.text		}	}}